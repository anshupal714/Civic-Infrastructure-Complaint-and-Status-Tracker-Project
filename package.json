{
  "name": "civic-tracker-root",
  "version": "1.0.0",
  "description": "Root package to easily start the backend server",
  "scripts": {
    "start": "cd backend && npm start",
    "seed-admin": "cd backend && npm run seed-admin"
  }
}
